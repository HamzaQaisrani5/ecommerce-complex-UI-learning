import 'package:flutter/material.dart';

class BottomNavItem {
  Icon activeIcon;
  Icon inActiveIcon;
  String label;
  BottomNavItem({
    required this.activeIcon,
    required this.inActiveIcon,
    required this.label,
  });
  
}
